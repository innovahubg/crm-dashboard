apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dashboard-dev
  template:
    metadata:
      labels:
        app: dashboard-dev
    spec:
      containers:
      - name: dashboard
        image: ihubg.azurecr.io/crm-dashboard-dev:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        - name: REACT_APP_API
          valueFrom:
            secretKeyRef:
              name: secrets-dashboard-dev
              key: REACT_APP_API
      imagePullSecrets:
      - name: acr-secret
      